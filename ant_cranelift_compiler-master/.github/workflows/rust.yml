name: Build ant_cranelift_compiler

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            bin: antc
          - os: windows-latest
            bin: antc.exe
          - os: macos-latest
            bin: antc

    steps:
      - uses: actions/checkout@v4

      - name: Build
        run: cargo build --release

      - name: Run tests
        run: cargo test --release

      - name: Inspect release output
        shell: bash
        run: ls -lah target/release

      - name: Upload binary
        uses: actions/upload-artifact@v4
        with:
          name: antc-${{ matrix.os }}
          path: target/release/${{ matrix.bin }}
